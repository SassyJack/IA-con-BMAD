name: security_vault
description: Security Vault package for PIN and Biometrics
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  crypto: ^3.0.7
  flutter_secure_storage: ^10.0.0
  local_auth: ^2.3.0

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.31.0
  very_good_analysis: ^10.2.0
